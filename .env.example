# ==============================================================================
# TradeSphere Environment Variables - Optimized for AWS 4KB Limit
# ==============================================================================

# ==============================================================================
# Aspire-Competitor Environment Variables
# For more detailed information on each variable, refer to the
# Environment Variable Reference Guide in the `docs` directory.
# ==============================================================================

# ------------------------------------------------------------------------------
# Core Configuration Variables
# These are the essential variables for setting up your company's brand identity.
# ------------------------------------------------------------------------------

# Company branding, appears in the page title and header.
VITE_COMPANY_NAME="TradeSphere Dev 1"

# The URL or path to your company's logo.
# Example (URL): https://your-cdn.com/logo.png
# Example (Local): /assets/branding/my-logo.svg
VITE_LOGO_URL="/assets/branding/default-logo.svg"

# The primary brand color for your application (in hex format).
VITE_PRIMARY_COLOR="#3b82f6"

# The secondary brand color for your application.
VITE_SECONDARY_COLOR="#6b7280"

# An accent color used for highlights and special notifications.
VITE_ACCENT_COLOR="#ef4444"

# The color used to indicate a successful action or state.
VITE_SUCCESS_COLOR="#22c55e"

# The initial greeting message the AI assistant shows to the user.
VITE_WELCOME_MESSAGE="Welcome! How can I help you today?"

# ------------------------------------------------------------------------------
# Industry Control Variables
# These variables control industry-specific theming and regional adaptations.
# ------------------------------------------------------------------------------

# Sets the overall industry theme.
# Options: "landscaping", "hvac", or "" (for the default tech theme).
VITE_INDUSTRY_TYPE=""

# Enables or disables automatic seasonal adjustments.
# Options: true, false
VITE_USE_SEASONAL_THEMES="false"

# Sets the geographic region for tailored seasonal messaging.
# Options: northeast, southeast, midwest, southwest, west, pacific_northwest
VITE_REGION="northeast"

# Sets the climate zone for more precise seasonal adjustments.
# Format: A valid USDA Plant Hardiness Zone (e.g., "7a", "8b").
VITE_CLIMATE_ZONE="6b"

# ------------------------------------------------------------------------------
# Visual Customization Variables
# Fine-tune the visual appearance and user experience.
# ------------------------------------------------------------------------------

# The name of a Lucide React icon for the header (https://lucide.dev/icons/).
VITE_HEADER_ICON="Briefcase"

# The animation effect when a user sends a message.
# Options: leaf_flutter, spark_burst, gear_spin, water_ripple, none
VITE_SEND_EFFECT="none"

# The animation displayed on the loading screen.
# Options: growth, building, gears, dots, default
VITE_LOADING_ANIMATION="default"

# The visual style of the chat message bubbles.
# Options: organic, geometric
VITE_MESSAGE_STYLE="geometric"

# The subtle background texture pattern.
# Options: subtle_organic, technical_grid, blueprint, none
VITE_BACKGROUND_PATTERN="none"

# ------------------------------------------------------------------------------
# Business Terminology Variables
# Customize the in-app language to match your business's terminology.
# ------------------------------------------------------------------------------

# A short description of your business type for the AI.
VITE_BUSINESS_TYPE="business_consultant"

# The term used to refer to the work being discussed (e.g., "project").
VITE_PROJECT_LANGUAGE="project"

# The term used for the pricing estimate (e.g., "estimate").
VITE_ESTIMATE_LANGUAGE="estimate"

# A comma-separated list of your main services.
VITE_PRIMARY_SERVICES="consulting,strategy,analysis"

# Custom placeholder text for the message input field.
VITE_PLACEHOLDER_EXAMPLES="e.g., 'I need help with my business strategy.'"

# The specialization level of your business.
# Options: full_service, specialist, consultant
VITE_SPECIALIZATION="consultant"

# The typical urgency of your services.
# Options: routine, seasonal, emergency
VITE_URGENCY_LEVEL="routine"

# ------------------------------------------------------------------------------
# ESSENTIAL Technical Integration Variables  
# ------------------------------------------------------------------------------

# Supabase Database
VITE_SUPABASE_URL="YOUR_SUPABASE_URL"
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Make.com Webhooks
VITE_MAKE_WEBHOOK_URL=YOUR_MAKE_WEBHOOK_URL
VITE_FEEDBACK_WEBHOOK_URL=YOUR_FEEDBACK_WEBHOOK_URL

# Dual Testing
VITE_ENABLE_DUAL_TESTING=true

# Native Pipeline Control (Phase 1: Native-First Migration)
VITE_USE_NATIVE_PRIMARY=true

# Customer Details Feature Control
VITE_ENABLE_CUSTOMER_DETAILS=true

# AI API Keys
VITE_AI_API_KEY=YOUR_CLAUDE_API_KEY
VITE_AI_PROVIDER="claude"

VITE_OPENAI_API_KEY_MINI=YOUR_OPENAI_API_KEY

# ------------------------------------------------------------------------------
# PWA App Identity Variables
# Differentiate deployed apps by name and icon
# ------------------------------------------------------------------------------

# The full app name shown in PWA manifest and app stores
VITE_PWA_NAME="Trade-Sphere Dev"

# The short app name for home screen icons (12 chars max recommended)  
VITE_PWA_SHORT_NAME="T-S Dev"

# App description in PWA manifest
VITE_PWA_DESCRIPTION="A revolutionary pricing tool for the trade industry."

# PWA icon files (place in public folder)
VITE_PWA_ICON_192="192x192-ts.png"
VITE_PWA_ICON_512="512x512-ts.png"

# Favicon and touch icons
VITE_PWA_FAVICON="favicon.ico"
VITE_PWA_APPLE_ICON="180x180-ts-apple.png"


# ------------------------------------------------------------------------------
# PHASE 4B: Stripe Payment Integration (ACH Bank Transfers with Plaid)
# ------------------------------------------------------------------------------

# 🔒 SECURITY: Stripe secret key must ONLY be server-side (Netlify functions)
# ⚠️ NEVER prefix STRIPE_SECRET_KEY with VITE_ - this exposes credentials to browser!
# All Stripe API calls must go through Netlify functions (not direct from frontend)

# Server-side Stripe API credentials (get from Stripe dashboard)
# Secret key: Used in Netlify functions for API calls
STRIPE_SECRET_KEY=sk_test_..._(sandbox) or sk_live_... (production)

# Client-side Stripe publishable key (safe to expose in frontend)
# Publishable key: Used in browser for Stripe.js and payment method collection
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_... (sandbox) or pk_live_... (production)

# Webhook secret for signature verification (from Stripe webhook settings)
# Create webhook endpoint in Stripe dashboard and copy the signing secret
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe webhook endpoint URL (register this in Stripe dashboard)
# Sandbox: https://full-code.netlify.app/.netlify/functions/stripe-webhook
# Production: https://app.tradesphere.com/.netlify/functions/stripe-webhook

# Supabase service role key (for webhook handler server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ------------------------------------------------------------------------------
# OPTIONAL: Plaid Configuration (for instant bank verification)
# ------------------------------------------------------------------------------

# Plaid is integrated through Stripe, no separate Plaid keys needed
# Stripe handles the Plaid connection automatically
# Instant verification: Bank accounts verified immediately (no micro-deposits)

# Note: ACH payments still take 4-5 business days to process (same as Dwolla)
# Verification is instant, but settlement time remains unchanged